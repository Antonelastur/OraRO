// Variantele de intrare folosite de listele și cardurile din agendă.
export const intrare = { hidden: { opacity: 0, y: 10 }, show: { opacity: 1, y: 0 } }
export const succesiune = { hidden: {}, show: { transition: { staggerChildren: 0.04 } } }
